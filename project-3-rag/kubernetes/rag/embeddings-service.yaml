apiVersion: v1
kind: Service
metadata:
  name: embeddings
  namespace: rag
  labels:
    app: embeddings
spec:
  type: ClusterIP
  selector:
    app: embeddings
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: metrics
      port: 9100
      targetPort: 9100
