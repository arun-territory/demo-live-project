apiVersion: v1
kind: ConfigMap
metadata:
  name: ml-inference-config
  namespace: ml-inference
data:
  MODEL_STORE_PATH: "/models"
  LOG_LEVEL: "INFO"
  MAX_WORKERS: "4"
  REQUEST_TIMEOUT_SECONDS: "30"
  ENABLE_METRICS: "true"
  METRICS_PORT: "9090"
