tasks:
  - name: Setup Weather Dashboard
    init: |
      npm install
    command: |
      echo "👋 Welcome to the Weather Dashboard Demo!"
      echo "🚀 Run these commands to start:"
      echo "npm start"

ports:
  - port: 3000
    onOpen: open-preview
    visibility: public

vscode:
  extensions:
    - dbaeumer.vscode-eslint
    - esbenp.prettier-vscode
